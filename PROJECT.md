# Project Context

## What this is

単体で完結するユーティリティスキルを集めたリポジトリ。
各スキルは相互依存を持たず、`gh skills` 経由で個別にインストールできる。

ワークフロー統合やメタスキルは [claude-skills](https://github.com/ba0918/claude-skills) に置く。
このリポジトリは「つまみ食い」前提の standalone スキル棚として棲み分ける。

## Stack and layout

- `skills/` — スキル本体。各スキルは `SKILL.md` を持ち、必要に応じて仕様準拠の任意ディレクトリ（`references/`, `scripts/`, `assets/`）を含む

## Commands

| Purpose | Command |
|---|---|
| Install | |
| Build | |
| Test | |
| Lint | `agentskills validate ./skills/<name>` |
| Run locally | |

`agentskills` は公式バリデータ [skills-ref](https://github.com/agentskills/agentskills/tree/main/skills-ref) が提供する CLI。`uv tool install skills-ref` で導入済み。

## Conventions specific to this project

### 言語方針

- `skills/` 以下の配布物（SKILL.md、references 等）は **英語** で書く。
  - 理由: description は毎セッション全スキル分ロードされ、本文は発火ごとにロードされるため英語の方がトークン効率が良い。加えて公開配布物として読者層が広がり、Claude 以外のエージェントでも指示追従が安定する。
- 翻訳版（SKILL-ja.md 等）は作らない。仕様に locale の仕組みはなくエージェントは読まないため、乖離コストだけが残る。
- 人間向けの日本語説明はリポジトリの README に集約する。
- リポジトリ運用ドキュメント（この PROJECT.md、README 等）は日本語でよい。

### スキル運用

- [agentskills.io の仕様](https://agentskills.io/specification) に準拠する。特にハマりやすい制約:
  - `name` は親ディレクトリ名と完全一致。小文字英数とハイフンのみ、64文字以内、先頭/末尾/連続ハイフン禁止
  - `description` は1024文字以内。「何をするか」+「いつ使うか」+ 発火のトリガーになる具体的キーワードを含める
  - SKILL.md 本文は500行以内を目安にし、詳細は `references/` へ分離する（progressive disclosure）
  - ファイル参照はスキルルートからの相対パスで、SKILL.md から1階層まで
- 各スキルは単体で完結すること。他スキルへの依存・共有契約は持たない。
- スキル本文はプラットフォーム非依存の自然言語で記述する。
  - NG: 固有のツール API 名、固有のモデル名、特定 CLI だけで通じる呼び出し形式
  - OK: 「シェルコマンドを実行する」「ファイルを読む」「ファイルを編集する」「サブエージェントに委譲する」

### 編集ルール

- 指示ファイルの役割分担:
  - `CLAUDE.md` は `@AGENTS.md` の1行だけを持つ薄いラッパー。プロジェクト指示を直接書かない。
  - `AGENTS.md` はインストール済みルールスキルから生成されるルーター。手で編集せず、スキルの追加・削除時に scaffold で再生成する。
  - プロジェクト固有の指示はこの `PROJECT.md` に書く。
- スキルを追加・変更したら `agentskills validate ./skills/<name>` を通してから完了とする。

## Constraints

## Glossary

### ba0918-teacher

- **teacher モード**: ba0918-teacher を `enable` してから `disable` するまでの、LLM がコードを書く側からユーザーが書くのを支える側に回る状態。セッションをまたがない。「学習モード」「Learning」とは呼ばない（Claude Code 標準の Learning output style と混同するため）。
- **学習テーマ**: teacher モードでユーザーが身につけたいこと（例: 「所有権とエラー処理」）。本質でない部分の境界を決める。「課題」「タスク」とは呼ばない（実作業そのものと混同するため）。
- **本質でない部分**: 学習テーマの外にある作業。LLM が宣言してから代行してよい。「雑用」とは呼ばない（テーマ次第で本題になるため）。
- **学習記録**: `~/.agents/ba0918-teacher/<名前>.md` に言語・分野ごとに置く、ユーザー個人のファイル。経験、次にやりたいこと、概念ごとの理解状況、セッションのまとめを持つ。リポジトリの外にある。「進捗」「ログ」とは呼ばない（作業の進み具合と混同するため）。
- **復習チェック**: 学習記録の「理解した」概念を、説明かコード片でユーザーに思い出させる確認。enable 時に次に復習する日が来たものを出す場合と、作業中に、次に復習する日が来ている概念がコードに必要になったときに聞く場合がある。「クイズ」「テスト」とは呼ばない（選択式や自動テストを連想させるため）。

### ba0918-opencode-exec

- **実行役スキル**: 呼び出し元が用意したプロンプトと環境で、特定のエージェント CLI にタスクを 1 つ実行させ、出力と変更を返すスキル。プロンプトの中身を書かず、結果を判定しない。「委譲スキル」とは呼ばない（ba0918-delegation の「委譲」は役割を決めて渡すところまで含む、より広い言葉のため）。
- **前置きのコマンド**: 呼び出し元が渡し、スキルが解釈せずに `opencode` の前に付けるコマンド列（例: サンドボックスの起動）。「ラッパー」とは呼ばない（PATH 上で本物のコマンドの代わりに置くシムと混同するため）。
- **変更一覧**: 実行の開始時と終了時を比べて、作業ディレクトリを含むリポジトリの git から見えるファイル（追跡中と、無視されていない未追跡）のうち追加・変更・削除されたもの。実行中に動いたコミットの変更も含む。誰の変更かは区別せず、git で無視されたファイルは含まない。「差分」とは呼ばない（`git diff` の出力と混同するため）。

### ba0918-rework

- **約束の変更**: I/F、UI の見た目、データ設計が変わる修正。公開 API の形の変更と削除、仕様の上で受け付けていた入力を拒むようになる修正を含む。人に聞く対象になる。「破壊的変更」とは呼ばない（バージョン管理の互換性の意味と混同し、内部のバグ修正まで含んで読めるため）。
- **聞く項目**: 約束の変更と、正しい挙動がリポジトリの中の根拠から決められない項目を合わせたもの。rework が人に聞くのはこれだけ。「要確認項目」とは呼ばない（確認一般と混同し、何でも聞いてよいと読めるため）。
- **仕様のテスト**: 変える挙動について、「こうあるべき」を仕様や呼び出し元などの根拠から書き、直す前に失敗させるテスト。コミットして残す。
- **足場のテスト**: 変えない挙動を守るテストが無いところで、修正の最中だけ今の挙動を固定し、宣言していない変化を見張るテスト。コミットしない（仕様の根拠と一致して仕様のテストに格上げしたものを除く）。「特性テスト」とは呼ばない（一般に特性テストは残すものであるため）。
